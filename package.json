{
  "name": "ruxjs",
  "version": "1.0.0",
  "description": "State-Manager for setting and detecting simple state changes.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "prepare" : "npm run build",
    "prepublishOnly" : "npm test && npm run lint",
    "format": "prettier --write \"src/**/*.ts\" \"src/**/*.js\"",
    "lint": "tslint -p tsconfig.json",
    "preversion" : "npm run lint",
    "version" : "npm run format && git add -A src",
    "postversion" : "git push && git push --tags"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/RickShaa/rux.js.git"
  },
  "author": "Rick Shaffer",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/RickShaa/rux.js/issues"
  },
  "homepage": "https://github.com/RickShaa/rux.js#readme",
  "devDependencies": {
    "prettier": "^2.7.1",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^4.7.4"
  }
}
